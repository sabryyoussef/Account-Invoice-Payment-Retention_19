<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_tpo_report_wizard_form" model="ir.ui.view">
        <field name="name">tpo.report.wizard.form</field>
        <field name="model">tpo.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Generate TPO Report">
                <sheet>
                    <group>
                        <group>
                            <field name="report_type"/>
                            <field name="project_id"/>
                            <field name="project_ids" widget="many2many_tags"/>
                        </group>
                        <group>
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="state_filter"/>
                        </group>
                    </group>
                    <group>
                        <field name="include_details"/>
                        <field name="include_summary"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_generate_report" string="Generate Report" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>

